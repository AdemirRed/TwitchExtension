# ── Bot Twitch ────────────────────────────────────────────
BOT_USERNAME=nome_do_bot
BOT_OAUTH_TOKEN=oauth:token_do_bot

# ── App OAuth (ExplorarLocais BOT) ────────────────────────
TWITCH_CLIENT_ID=c5yhiby5xsxplfuuj1xuszg0j168m7
TWITCH_CLIENT_SECRET=assd8f7a57ogk2f7dl2q2t1xue3r20

# ── Banco de Dados (Supabase) ─────────────────────────────
# Pegue em: supabase.com → seu projeto → Settings → Database → Connection string → URI
DATABASE_URL=postgresql://postgres:[senha]@[host]:5432/postgres

# ── Servidor ──────────────────────────────────────────────
PORT=8080
# URL pública do app (Railway preenche automaticamente)
BASE_URL=https://seu-app.railway.app
# Chave aleatória para sessões — gere com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=troque_por_string_aleatoria_longa
